services:
  lightspot-redis:
    image: redis:7.2-alpine
    container_name: lightspot-redis
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - lightspot-redis-data:/data
    restart: unless-stopped

volumes:
  lightspot-redis-data:
